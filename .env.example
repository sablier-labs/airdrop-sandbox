# =============================================================================
# Sablier Airdrop Template - Sensitive Environment Variables
# =============================================================================
# This file contains ONLY sensitive data (API keys, secrets, deployment tokens)
# Copy to .env.local and configure with your actual values
# Never commit .env.local to version control

# =============================================================================
# CONFIGURATION ARCHITECTURE
# =============================================================================
# Environment variables (.env) = Sensitive data only (API keys, secrets)
# JSON config files = Non-sensitive configuration (features, settings, networks)
#
# Configuration files:
# - config/app.config.json      - App settings (networks, features, UI theme)
# - config/campaign.config.json - Airdrop campaign details
#
# Migration from old system:
# - All feature flags → config/app.config.json
# - All network settings → config/app.config.json  
# - All UI settings → config/app.config.json
# - Campaign details → config/campaign.config.json
# - Only sensitive data remains in .env

# =============================================================================
# REQUIRED: Web3 API Keys
# =============================================================================

# WalletConnect Project ID (Required)
# Get your project ID from: https://cloud.walletconnect.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id_here

# Alchemy API Key (Required for RPC endpoints)
# Get your API key from: https://www.alchemy.com
NEXT_PUBLIC_ALCHEMY_API_KEY=your_alchemy_api_key_here

# =============================================================================
# OPTIONAL: Analytics & Monitoring
# =============================================================================

# Google Analytics 4 Measurement ID
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry DSN for Error Monitoring
# NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn_here

# =============================================================================
# OPTIONAL: Deployment Tokens
# =============================================================================

# Vercel deployment configuration (only needed for automated deployments)
VERCEL_ORG_ID=your_vercel_org_id
VERCEL_PROJECT_ID=your_vercel_project_id
VERCEL_TOKEN=your_vercel_token
